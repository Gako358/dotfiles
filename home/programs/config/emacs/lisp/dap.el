;;; dap.el --- DAP configuration
;;; Commentary:
;;; Code:

;; Enable Dap
(require 'dap-mode)
(dap-mode 1)
(dap-ui-mode 1)
(dap-tooltip-mode 1)
(tooltip-mode 1) ;; Use tooltips for mouse hover
(dap-ui-controls-mode 1)

;;; dap.el ends here
